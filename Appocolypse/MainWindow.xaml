<Window x:Class="Appocolypse.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Appocolypse"
        mc:Ignorable="d"
        Title="{Binding TitleText}"
        Height="450"
        Width="450"
        >
    <Grid Background="{Binding BgColorHex}">
        <Grid.RowDefinitions>
            <RowDefinition Height="24"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Menu Bar -->
        <DockPanel>
            <Menu FontSize="14">
                <MenuItem Header="File" Padding="8,2,8,2">
                    <MenuItem Icon="" IsCheckable="False" Header="Exit" Click="MainMenuItem_ExitClicked"/>
                </MenuItem>
            </Menu>
        </DockPanel>
    </Grid>
</Window>
